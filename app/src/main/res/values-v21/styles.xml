<resources>

    <!--欢迎界面theme-->
    <style name="WelTheme" parent="AppTheme">
        <item name="android:windowTranslucentStatus">true</item>
        <!-- 启动背景图片 -->
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>

    <style name="MyProgressBar" parent="Base.Widget.AppCompat.ProgressBar">
        <item name="android:indeterminateTint">@color/secColor</item>
        <item name="android:indeterminateTintMode">src_atop</item>
    </style>

    <style name="MyMaterialDialog" parent="android:Theme.Material.Light.Dialog">
        <item name="android:colorPrimary">@color/secColor</item>
        <item name="android:colorPrimaryDark">@color/secColor</item>
        <item name="android:colorAccent">@color/secColor</item>
    </style>

</resources>